apiVersion: v1
kind: Secret
metadata:
  name: {{ include "location-master-service.fullname" . }}-secret
  labels:
    {{- include "location-master-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Database password reference (override with external secret in production)
  # This is a placeholder - should be overridden using --set or external secret management
  SPRING_DATASOURCE_PASSWORD: "changeme"
